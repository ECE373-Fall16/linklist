<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_host_connect"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="soundsync.linklist.HostConnect">

    <TextView
        android:id="@+id/pageTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="25dp"
        android:text="Host New Lobby"
        android:textAllCaps="true"
        android:layout_centerHorizontal="true"
        android:layout_alignParentTop="true"/>
    <TextView
        android:id="@+id/lobbyText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="20dp"
        android:text="Lobby"
        android:textColor="@color/colorPrimary"
        android:textSize="20dp"
        android:layout_below="@id/pageTitle"
        android:layout_centerHorizontal="true"/>

    <EditText
        android:id="@+id/lobbyName"
        android:layout_below="@id/lobbyText"
        android:layout_width="match_parent"
        android:hint="choose lobby name"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/sizeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Size"
        android:textColor="@color/colorPrimary"
        android:textSize="20dp"
        android:layout_below="@id/lobbyName"
        android:layout_centerHorizontal="true"/>

    <EditText
        android:id="@+id/lobbySize"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="choose lobby size"
        android:layout_below="@id/sizeText"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Connect"
        android:onClick="hostPlay"/>

</RelativeLayout>
